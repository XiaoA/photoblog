<%= render 'navigation' %>
<div class="container-fluid">

  <p id="notice"><%= notice %></p>

  <div class="text-center center-block">
    <h1><%= @post.title %></h1>
    <%= image_tag(@post.image.full_size.url) %>
    <h4><%= @post.description %></h4>
    <%= link_to 'Edit', edit_post_path(@post) %> |
    <%= link_to 'Back', posts_path %>
  </div>
</div>
